"pub trait PSP34Enumerable : PSP34\n{\n    #[doc =\n    \" Returns a token `Id` owned by `owner` at a given `index` of its token list.\"]\n    #[doc =\n    \" Use along with `balance_of` to enumerate all of ``owner``\\'s tokens.\"]\n    #[doc = \"\"] #[doc = \" The start index is zero.\"] #[ink(message)]\n    #[ink(selector = 1003468049u32)] fn\n    owners_token_by_index(& self, owner : AccountId, index : u128) -> Result <\n    Id, PSP34Error > ;\n    #[doc =\n    \" Returns a token `Id` at a given `index` of all the tokens stored by the contract.\"]\n    #[doc = \" Use along with `total_supply` to enumerate all tokens.\"]\n    #[doc = \"\"] #[doc = \" The start index is zero.\"] #[ink(message)]\n    #[ink(selector = 3439542480u32)] fn token_by_index(& self, index : u128)\n    -> Result < Id, PSP34Error > ;\n}"